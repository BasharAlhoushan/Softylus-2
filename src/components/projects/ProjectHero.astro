---
// ProjectHero.astro - Hero section for the projects page
---

<section class="relative py-24 md:py-32 px-4 bg-gradient-to-b from-gray-50 to-white overflow-hidden">
  <!-- Background Elements -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute inset-0 opacity-[0.03]" aria-hidden="true">
      <div class="absolute inset-0 bg-grid-pattern transform -skew-y-6"></div>
    </div>
    
    <!-- Geometric shapes -->
    <div class="absolute top-1/4 right-1/4 w-64 h-64 border border-black/5 rounded-full opacity-20 transform rotate-12"></div>
    <div class="absolute bottom-0 left-1/3 w-80 h-80 border border-black/5 rounded-full opacity-20 transform -rotate-6 translate-y-1/2"></div>
  </div>

  <div class="container mx-auto relative">
    <!-- Breadcrumb -->
    <div class="mb-6 text-sm" data-aos="fade-up">
      <nav class="flex" aria-label="Breadcrumb">
        <ol class="flex items-center space-x-2">
          <li>
            <a href="/" class="text-gray-500 hover:text-black transition-colors">Home</a>
          </li>
          <li class="flex items-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
            <span class="ml-2 text-black font-medium">Projects</span>
          </li>
        </ol>
      </nav>
    </div>

    <!-- Hero Content -->
    <div class="max-w-4xl" data-aos="fade-up">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-display font-bold leading-tight mb-6">
        Our Creative <br/>
        <span class="relative inline-block">
          Digital Portfolio
          <svg class="absolute w-full bottom-1 left-0 -z-10" height="10" viewBox="0 0 200 10" xmlns="http://www.w3.org/2000/svg">
            <path d="M0,5 Q40,0 80,5 T160,5 T240,5" stroke="rgba(0,0,0,0.3)" stroke-width="2" fill="none"/>
          </svg>
        </span>
      </h1>

      <p class="text-xl text-gray-600 mb-8 max-w-2xl" data-aos="fade-up" data-aos-delay="100">
        Explore our diverse collection of projects that showcase our expertise in crafting exceptional digital experiences across various industries.
      </p>

      <!-- Stats -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 mt-12" data-aos="fade-up" data-aos-delay="200">
        <div class="stat-item">
          <div class="text-3xl font-display font-bold mb-2">42+</div>
          <div class="text-sm text-gray-600">Completed Projects</div>
        </div>
        <div class="stat-item">
          <div class="text-3xl font-display font-bold mb-2">18</div>
          <div class="text-sm text-gray-600">Industries Served</div>
        </div>
        <div class="stat-item">
          <div class="text-3xl font-display font-bold mb-2">96%</div>
          <div class="text-sm text-gray-600">Client Satisfaction</div>
        </div>
        <div class="stat-item">
          <div class="text-3xl font-display font-bold mb-2">12</div>
          <div class="text-sm text-gray-600">Design Awards</div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .bg-grid-pattern {
    background-image: 
      linear-gradient(to right, rgba(0,0,0,0.05) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(0,0,0,0.05) 1px, transparent 1px);
    background-size: 40px 40px;
  }

  .stat-item {
    position: relative;
  }

  .stat-item::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background-color: black;
    transition: width 0.3s ease;
  }

  .stat-item:hover::after {
    width: 100%;
  }

  @media (prefers-reduced-motion: reduce) {
    .stat-item::after {
      transition: none;
    }
  }
</style>